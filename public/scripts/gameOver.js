if(player1.isAlive() === true && player2.isAlive() === true) {
    return
} else if (player1.isAlive === false) {
  //fetch request for gameOver screen with 'Player 2 Wins' string 
  return 
} else {
    // fetch request for gameOVer screen with 'Player 1 Wins' string
    return
}